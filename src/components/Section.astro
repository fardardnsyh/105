---
interface Props {
    title: string;
}

const { title } = Astro.props;
---

<section class="max-w-3xl mb-6 mx-auto">
    {title && <h2 class="text-xl font-bold mb-2">{title}</h2>}
    <slot />
</section>
